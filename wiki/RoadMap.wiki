#summary Plans and ideas


We're at version 0.1 now, to get to 1.0 we need a stable version.

Features:
  * Validation: automatically generate client side validation. Also do server side validation for common stuff (email, required, ...). Also allow user to define a validation function that is called.
  * Ajax. Deal with ajax somehow.